{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "check-types": "turbo check-types"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.5.1",
    "turbo": "^2.4.4",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@6.14.13",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@repo/shared": "^1.0.0",
    "firebase": "^11.3.1"
  }
}
